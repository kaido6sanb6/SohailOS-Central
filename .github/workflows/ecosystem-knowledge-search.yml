name: ecosystem-knowledge-search

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/ecosystem-knowledge-search.yml"

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "ecosystem knowledge workflow is valid"
