{
  "name": "sohailos-central-cloudflare-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "cloudflare:check": "wrangler check",
    "cloudflare:dry-run": "wrangler versions upload --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.141.0"
  }
}
